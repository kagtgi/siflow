# Colab A100 (CUDA 12) extras.
#
# Colab already ships a working CUDA build of torch -- do NOT `pip install torch`
# here or you risk replacing it with a CPU/incompatible wheel. Run:
#
#     !pip install -e .            # installs the deps below via pyproject
#     !pip install -r requirements-colab.txt
#
transformers>=4.44
datasets>=2.20
safetensors>=0.4
omegaconf>=2.3
accelerate>=0.30
einops>=0.7
sentencepiece>=0.2          # Gemma / Qwen tokenizers
mauve-text>=0.4             # MAUVE metric
sacrebleu>=2.4              # self-BLEU diversity
scikit-learn>=1.3
matplotlib>=3.7
tqdm>=4.66
